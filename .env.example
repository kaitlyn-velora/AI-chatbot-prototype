# Copy to .env.local and add your key. Used only when running `npm run dev` (Vite dev server).
# Do not commit real keys. Static / Netlify builds do not include this API — use a serverless proxy for production.

GEMINI_API_KEY=AIzaSyAtYHhlxh4_cb4pcAYnnPTjmZe4SG6aTFc

# Optional (default: gemini-2.5-flash)
# GEMINI_MODEL=gemini-2.5-flash
